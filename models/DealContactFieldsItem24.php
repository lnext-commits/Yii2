<?php

namespace models;

use bitrix\components\CRest;
use Yii;
use yii\db\ActiveRecord;

/**
 * Products model
 *
 * @property integer $id
 */

class DealContactFieldsItem24 extends ActiveRecord
{
    /**
     * @inheritdoc
     */
    public static function tableName()
    {
        return '{{%b24_deals_contact_fields}}';
    }

    public function beforeSave($insert)
    {
        return parent::beforeSave($insert); // TODO: Change the autogenerated stub
    }

    static public function loadDealContactFieldsItemFromB24($dealId)
    {
        //$dealCFields = CRest::call('crm.deal.userfield.get' , ['id'=>$dealId]);
        //$dealCFields = CRest::call('crm.deal.userfield.list');
/*
        if ($dealCFields && isset($dealCFields['result'])) {
            foreach ($dealCFields['result'] as $k => $field) {
                if (!$cfield = self::findOne(['name' => $k])) {
                    $cfield = new self();
                }
                $cfield->name = $k;
                $cfield->title = $field['title'];
                $cfield->save();
            }
        }
*/
        //return $dealCFields;
    }

}